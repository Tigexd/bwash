image: alpine:latest
pages:
  stage: deploy
  script:
    - echo 'Hello World! Nothing else to do.'
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "$CI_DEFAULT_BRANCH"